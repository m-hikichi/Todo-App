services:
  todo-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: todo_app:dev
    container_name: todo_app
    ports:
      - "8080:8080"
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "8080"
      APP_DATA_DIR: "/app/data"
    volumes:
      - ./data:/app/data
